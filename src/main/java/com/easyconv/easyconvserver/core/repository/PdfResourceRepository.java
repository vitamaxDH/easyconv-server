package com.easyconv.easyconvserver.core.repository;

import com.easyconv.easyconvserver.core.entity.PdfResource;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PdfResourceRepository extends JpaRepository<PdfResource, String> {
}
